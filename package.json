{
  "name": "fish-target-rc",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "npm run build && node --test",
    "test:syntax": "node --check data.js && node --check products.js && node --check app.js && node --check tackle.js && node --check pwa.js && node --check sw.js && node --check scripts/build.mjs && node --check scripts/version-contract.mjs"
  }
}
